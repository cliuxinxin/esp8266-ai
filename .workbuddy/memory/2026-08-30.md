- 名人名言 Mac 端排版改为自适应字号：根据正文长度在限定范围内选择最大可容字号（中文 15–30pt、英文 13–26pt），正文块在 208×150 区域内垂直居中；1–2 行居中，超过 2 行左对齐；作者字号在 [13,12,11,10] 中选择最大不溢出的。新增 `QuoteLayout.swift` 统一设备位图与镜像预览的几何和字体，确保两者一致。`QuoteMonitor.isDisplayable` 改为基于 `QuotePageLayout.fits` 判断。`swift build/test --disable-sandbox` 通过，35 个测试全绿。渲染预览图保存在 `outputs/quote-layout-preview/`。

- 部署/验证要点（本次踩到的坑，下次直接用）：
  - Mac 端必须加 `--disable-sandbox`（`swift build -c release --disable-sandbox`），否则 SwiftPM 编译 Package.swift 时 `sandbox-exec: Operation not permitted` 报 "Invalid manifest"。
  - 本机没有 LaunchAgent，服务是手动跑 `.build/release/AIClockBridge`。重启要用 `python3 -c "subprocess.Popen([...], start_new_session=True)"` 脱离会话启动，否则 agent 回合结束进程会被带走。日志 `/tmp/aiclock.log`。
  - 烧录：`cd firmware && source .pio-venv/bin/activate && pio run -t upload`（串口 `/dev/cu.usbserial-130`，约 43 秒，852832 字节）。
  - 本地调试 HTTP 必须加 `--noproxy '*'`，环境里设了 `HTTP_PROXY=127.0.0.1:60254`，否则访问局域网设备会得到假 502。
  - 名言页位图是设备通过 HTTP 从 Mac 拉的（`drawQuoteScreen` 要求 `WiFi.status()==WL_CONNECTED`），**USB 有线模式不会传位图**。设备当前只有串口在线（`192.168.31.29` no route to host），所以名言页在设备屏幕上显示不出来，只能在 Mac 菜单栏镜像里看。

- 功能摸底结论（供后续选型参考）：
  - petdex 提供 **9 种动画**（idle / running-right / running-left / waving / jumping / failed / waiting / running / review，见 `PetdexService.states`），但 `buildGif` 只裁 1 行上传，设备每槽只有 `/c.gif`、`/x.gif` 一个循环，**其余 8 种等于白下载**。
  - 事件词汇表已在 `StatusReader`：working = UserPromptSubmit/PreToolUse/PostToolUse/SubagentStart/SubagentStop/PreCompact/PostCompact/WorktreeCreate；idle = Stop/SessionEnd/SessionStart；attention = Elicitation/PermissionRequest/Notification(permission)。**没有 error/failed 事件**，要做"失败"表情得扩展 hook。
  - 硬件无按键（config.h 未定义任何 button/KEY 引脚），交互只能靠网页或 Mac。
  - flash：app 852,832 / 1,044,464（81.6%），LittleFS 走 nodemcuv2 默认 4M1M 约 3MB，**空间充裕**。
  - `/status` 在有线模式走串口 `#STATUS`，所以往 status JSON 加字段是**不依赖 WiFi** 的通道；而位图/精灵图上传只走 HTTP。

- Mac 磁盘清理只读扫描（用户要求"只出报告、手动核对"，**未删除任何文件**）。报告：`outputs/mac-cleanup-report-2026-08-30.md`。
  - 结论：APFS 数据卷 181 GiB / 228 GiB（99%），仅剩 **2.3 GiB**，危险水位。
  - 最大头是 **iOS 模拟器 `/Library/Developer/CoreSimulator` ≈ 79 GB**（Volumes: iOS_22G86 19G / iOS_22D8075 18G / iOS_23F77 16G；Cryptex/Images/bundle 16G；Caches/dyld 10G 纯缓存可秒删）。装有 3 个运行时（18.3/18.6/26.5）+ 22 台设备，全部 Shutdown。
  - 其余大户：`~/.ollama/models` 14G（gemma4:e4b-mlx 8.8G、qwen3.5-abliterated:9b 6.6G）、`~/Library/Caches` 9.3G（ShipIt 升级残留类占大头）、`~/Library/Containers` 14G（QQ 5.2G / 微信 3.3G，必须应用内清、勿直接删目录）、`~/Library/Application Support` 16G。
  - 预计总可回收 60–80 GB；推荐顺序：ShipIt 残留 → dyld 缓存 → 删 1~2 个模拟器运行时 → 微信/QQ 应用内清 → 删 Ollama 模型。
  - **环境坑**：本机会话默认沙箱下 `du`/`find` 访问 `~/Downloads`、`~/Library` 等个人目录会被 SIGKILL（exit 137），必须 `dangerouslyDisableSandbox: true`（本质是 macOS TCC）。全盘 `find ~ -size +1G` 仍会被 kill，改用 `du -sh <目录>/*` 逐层下钻更稳。

- 同一天用户确认后**实际执行了清理**（第二阶段），共回收 **31 GiB**：181Gi/99% → **150Gi/82%，可用 2.3Gi → 35Gi**。报告文件已追加"一·补、执行记录"章节。
  - 命令记录（可复用）：`ollama rm <model>`；`xcrun simctl delete all`（一次清空所有设备，比逐个 UUID 快）；`xcrun simctl runtime delete <UUID>` 删运行时（**异步**，删除后 `runtime list` 会短暂显示 `Deleting` 状态，过一会儿再查才是 0）。
  - 关键发现：**dyld 共享缓存会随运行时一起被清空**，不需要 `sudo rm -rf /Library/Developer/CoreSimulator/Caches/dyld`。而本机 `sudo` 需要密码，agent 无法执行——好在这步没用上。
  - 本环境的 `rm -rf` 会被 WorkBuddy 的 safe-delete 包装器拦截并尝试移入废纸篓；对 root 所有的系统目录（如 `/Library/...`）会失败并报 `SAFE_DELETE_FAIL_CLOSED` / `FSMoveObjectToTrashSync failed (status -5000)`。**结论：系统级目录清理必须走专用命令（如 simctl），不要指望 rm。**
  - 剩余可清项（本次未做）：`~/Library/Caches` 还有 4.6G（Arc 977M / Google 806M / ms-playwright 1.0G / WorkBuddy BundleMigration 1.3G），微信 QQ 容器 ~9.6G（须应用内清）。

- 用户实际使用面（从本机 `/status` 真实配置读出）：事件项只开了 **codex**（claude/approval/music 全关）；定时项只开 **quote 与 weather，两者都是 间隔 120s / 时长 30s**；net、stock 关闭。判断：用户在用"快速轮播"凑常驻。
- 设备网络：17:10 前后自行连回 WiFi（SSID `2702`，IP 192.168.31.29，bridge 192.168.31.100:8765，brightness 53）。已用 `POST /api/display mode=quote` 把新排版的名言页推上真机屏幕。
- 名言刷新节奏（实测）：代码里唯一自动触发源是 `QuoteMonitor.swift:104` 的 `Timer.scheduledTimer(withTimeInterval: 30 * 60)`；另外每次桥接启动会立刻拉一条新的（`start()` 里的 `refresh`），菜单栏「换一句」= `refresh(force: true)`。**已实测：连续采样 7 分 41 秒 text_rev 未变化**，与 30 分钟一致。缓存里曾出现两条相差 4:43 的记录，是这轮调试反复重启桥接导致的，非真实节奏。
  - 重要换算：内容 30 分钟一换，但用户把名言页设成 120s 间隔 / 30s 时长 → **同一句话大约会看到 15 遍**。所以「此刻」页若要常驻，名言刷新间隔必须一起调快（建议 5–10 分钟），否则常驻的就是一张半小时不变的静态卡片。

- **已实施**：名言刷新间隔 30 分钟 → **5 分钟**。`QuoteMonitor.swift` 新增 `private static let refreshInterval: TimeInterval = 5 * 60`（放在 `cacheVersion` 旁边，带注释说明原因），`start()` 的定时器改用它。两个来源交替请求，实际每个源约 6 次/小时。
  - 验证：release 编译通过，35 个测试全绿；重启后实测采样 421 秒，**rev 27 → 28 间隔 285 秒**（≈4分45秒，符合 5 分钟）。设备 `mode=quote`、已同步到 rev=28。
  - 未做（可选项）：候选池 / 断网时在最近几条里轮换，避免 API 挂掉时画面冻住；以及把间隔做成设置项而非常量。

- **「此刻 NOW」页已实现**（用户确认方案 A：新增 MODE_NOW，名言为主 + 天气条 + Codex 周额度/重置倒计时；本期只做手动固定，自动轮播二期）：
  - Mac 端新增 `NowPageLayout.swift`（合成版式：头部 NOW+时间、正文自适应 14–26zh/12–24en、作者、分隔线、天气两行、CODEX 用量条+百分比+重置 `x时y分/ x天y时`）+ `NowPageMonitor.swift`（每 10s 合并 quote/weather/codex 快照，rev 变化才重渲染）+ `RGB565.swift`（共用 CGContext→RGB565，QuoteMonitor.renderQuotePage 同步改为调它）；`main.swift` 挂 `/now`（`{"rev":N,"available":true}`）与 `/now/text.raw`；镜像 `MirrorPopover` 加「此刻」分段与 `drawNowScene`；菜单「屏幕显示」加「此刻」。
  - 固件 `main.cpp`：`MODE_NOW` 枚举 + 三处 `"now"` 映射（serial #CMD / POST /api/display / 网页 select + displayModeName）；把 `drawQuoteScreen` 泛化为 `streamBitmapPage(url,state,drawn,dirty,drawnRev,payloadRev)`，quote 与 now 共用；新增 `pollNow()`/`nowCanEnterMode()`/`drawNowScreen()`；`effectiveMode` 固定模式守卫 + `loop()` 轮询/切换/绘制分支各一处。
  - 验证：`swift build -c release --disable-sandbox` 通过、`swift test` 45 全绿（新增 `NowPageLayoutTests` 10 个，覆盖天气串/重置格式/rev/位图字节数）；`pio run` 编译成功（Flash 81.3% / RAM 55.4%）。
  - **已部署**：桥接重启（PID 99174，`/tmp/aiclock.log`，`/now` 返回 rev、`/now/text.raw` 115200 字节）；固件已烧录（41.8s，hash 校验通过，硬复位重连 WiFi `2702` / IP 192.168.31.29 / 桥接 192.168.31.100:8765）；设备已 `POST /api/display mode=now`，`effective=now` 生效。注：FW_VERSION 仍为 0.4.13（未改版本号）。
  - 约束（与 quote 页相同）：**有线 USB 模式不拉位图**，「此刻」需 WiFi；Codex 已无 5h 窗口只显示周额度。
  - 二期候选（未做）：把「此刻」加入 `auto_display_logic.h` 定时调度 + Mac 自动显示设置一行；名言按天气/时段切一言 `c=` 分类；「此刻」内直接点「换一句」时设备约 10s 内刷新（rev 含 quote.textRev）。
